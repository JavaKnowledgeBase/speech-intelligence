fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
supabase>=2.7.4

# Optional — required only when FILTER_SERVICE_URL is set and calls go over HTTP
httpx>=0.27.0

# Development / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
